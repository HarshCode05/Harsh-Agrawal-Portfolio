// This file sets up Vercel Analytics to track who visits your portfolio.
// Vercel Analytics is FREE on the Hobby (free) plan.
// It shows: page views, unique visitors, top pages, countries, devices.

// Step 1: Install the package (already added to package.json below)
// Run: npm install @vercel/analytics

// Step 2: The Analytics component is already added to layout.tsx
// No API key needed — Vercel detects it automatically when deployed.

// Step 3: After deploying, go to your Vercel dashboard → your project → Analytics tab

export {}
